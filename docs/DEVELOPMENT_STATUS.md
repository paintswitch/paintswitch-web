# PaintSwitch development status

Last repository inspection: 2026-08-09

Last documentation-completeness audit: 2026-08-04

Last local application verification: 2026-08-08

Last external platform verification: 2026-08-08

Last external deployment verification: 2026-08-08

Last external GoHighLevel verification: 2026-08-08

Repository: `paintswitch/paintswitch-web`

This document separates facts verified directly from the current repository from reports that have not been reverified during this documentation update.

## Verified repository facts

### Repository and application

- The configured Git remote is `https://github.com/paintswitch/paintswitch-web.git`.
- The package name is `paintswitch-web`, version `0.1.0`, and the package is private.
- The repository contains a Next.js App Router application under `src/app`, written in TypeScript and React.
- Manifest versions are Next.js `16.3.0`, React `19.2.4`, React DOM `19.2.4`, TypeScript `^5`, Tailwind CSS `^4`, and ESLint `^9`.
- Lockfile-resolved development versions include TypeScript `5.9.3`, Tailwind CSS `4.3.3`, and ESLint `9.39.5`. See `TECHNICAL_ARCHITECTURE.md` for the complete verified dependency summary.
- The implemented authored page routes are `/`, `/privacy`, and `/terms`; the App Router also serves the favicon and framework-provided error/not-found behavior. One authored dynamic route, `/api/leads`, validates lead requests and invokes the delivery service. Commit `3bac3ba` on `codex/lead-generation-beta` was deployed to protected Vercel Previews for a controlled enabled-delivery test and subsequent fail-closed restoration. The Preview environment setting and latest branch Preview have `LEAD_DELIVERY_ENABLED=false`; Production also has `false`. The earlier immutable enabled Preview remains access-protected.
- The repository has `dev`, `build`, `start`, `lint`, `test`, and `typecheck` scripts.

### Implemented user experience

- A responsive, single-page PaintSwitch marketing frontend is implemented locally.
- It includes a sticky header, desktop and `<details>` mobile navigation, a skip link, hero, trust bar, four approved service cards, beta-accurate process steps, brand-benefit section, DMV-wide individual service-area review copy with Virginia prioritized, a branded quote-request form, working Privacy and Terms pages, and footer.
- Interior, exterior, cabinet, and commercial are the only service categories shown. Standalone Drywall Repair and the placeholder review section have been removed.
- Primary actions use “Request a Quote” and navigate to the branded form. Metadata and page copy identify the four categories and state that service availability and pricing are confirmed after review.
- The form frontend collects name, phone, email, project ZIP code or address, service type, project description, and contact preference. It uses the same parser as the server before sending, shows field-specific accessible errors, focuses the first invalid control, applies length limits, provides submitting/success/failure presentation, retains a per-attempt UUID across an unchanged retry, clears that UUID after a server `409` submission-ID conflict, applies a 50-second browser timeout, and captures bounded `utm_source` / `utm_campaign` values. Its explicit native `POST` fallback prevents customer details from falling into a URL query if JavaScript is unavailable, although the JSON-only endpoint intentionally rejects that non-JavaScript encoding.
- The form includes the approved 18+ acknowledgment, project-specific contact notice, `Text`-preference/automated-SMS boundary, and working Privacy/Terms links. It does not display an instant price, collect payment, enable checkout, or promise customer-selected scheduling.
- The frontend was inspected locally at desktop and mobile sizes on 2026-08-03 and rechecked in Chrome at a 390 × 844 mobile target on 2026-08-04. The homepage, Privacy, and Terms routes had no horizontal overflow; footer and form legal links reached the correct pages; and the form link did not submit the form. The earlier mobile-menu, validation-focus, skip-link, and recoverable-failure checks remain valid. This is responsive and interaction evidence, not cross-browser, screen-reader, production, or end-to-end delivery acceptance.
- **Local editorial redesign working tree — implemented, locally verified, and owner-approved as the beta design candidate 2026-08-09; hosted deployment pending:** The homepage, shared navigation/buttons/footer/headings/service/process components, quote-form presentation, and legal-page presentation use a warm-neutral/deep-teal editorial system based on D-055 through D-058. `globals.css` defines the supplied soft-cream, greige, taupe, teal, and charcoal values plus system sans and Georgia/Times editorial-serif stacks. The hero imports `public/images/paintswitch-color-study.png` locally through `next/image`, and the rendered figure explicitly labels it “Illustrative color study” and “Not a customer project.” It is generated/local concept imagery, not authentic PaintSwitch customer work, a testimonial, or project-history evidence. The header remains a text-only wordmark; no final roller-logo asset is present. Sixty-two tests, lint, TypeScript checking, and the Next.js `16.3.0` production build pass. Browser QA at 1440×1000 and 390×844 found no horizontal overflow, verified the local hero image, mobile navigation open/close and anchor behavior, empty-form validation/focus behavior, responsive quote-form geometry, Privacy and Terms rendering, and zero page warnings or errors. Key approved text/background pairs measured from 4.52:1 to 7.79:1, including composited translucent text. The revised headline sizes measure within the 280-pixel content width at 320 pixels, but full 320-pixel browser/zoom testing remains open. D-058 records the owner's phone-based visual approval of this exact local layout, current Georgia/system typography, current token assignments, interim wordmark, and labeled hero as the lead-generation beta candidate. It does not establish a commit, hosted Preview, Production release, final paint-roller logo, customer-work claim, exact warm-gold value, final supporting copy beyond the displayed copy, or promoted-traffic approval.
- **D-059 launch/copy update — implemented locally 2026-08-09; hosted release pending:** The owner targeted the full D-058 editorial design plus safeguarded `Switch bot` for launch by the weekend of 2026-08-15 through 2026-08-16. `src/app/terms/page.tsx` no longer publishes the former five-minute or 9:00 a.m. first-contact targets; it now says PaintSwitch follows up as soon as reasonably possible and guarantees no specific timeframe. `tests/site-content.test.mjs` asserts the replacement copy and rejects either exact timing. The internal GoHighLevel five-minute Alex escalation is unchanged. All 62 tests, lint, TypeScript checking, and the Next.js `16.3.0` production build pass after this change. No commit, hosted Preview, Production deployment, Production lead delivery, or combined bot/design acceptance has occurred for this working tree.
- **Committed chatbot integration — verified through hosted Preview 2026-08-07:** Commit `649f06d` on `codex/lead-generation-beta` contains `src/components/highlevel-chat-widget.tsx`, which uses `next/script` with `strategy="lazyOnload"`, public widget ID `6a75455fa70a87ea8ede056f`, and a `HIGHLEVEL_CHAT_WIDGET_ENABLED === "true"` gate. `src/app/page.tsx` mounts it only on the homepage. `next.config.ts` conditionally adds exact `https://widgets.leadconnectorhq.com`, `https://services.leadconnectorhq.com`, and `https://stcdn.leadconnectorhq.com` CSP origins when the flag is true. The commit also contains the August 6 chat-policy expansion and source-contract tests. Sixty-one tests, lint, type checking, and a Next.js `16.3.0` chat-enabled production build passed locally. The commit was pushed, and PR #2 passed GitHub `Verify`, `Vercel`, and `Vercel Preview Comments`. Hosted Preview `https://paintswitch-9vzqs7zmi-paint-switch.vercel.app` loaded the widget only on the homepage without a site/widget browser error. This Preview evidence did not verify Production runtime; later Production deployment and opening evidence is recorded below.
- **Committed WSS transport candidate and hosted retest — verified 2026-08-08:** After inspection of HighLevel's public widget bundle showed Socket.IO at marketplace `https://services.leadconnectorhq.com`, path `/sockets-live-chat/socket.io`, with WebSocket/polling transports, commit `85117a1` adds exact `wss://services.leadconnectorhq.com` to chat-enabled `connect-src` without a wildcard. The source-contract test asserts the exact WSS origin. The commit was pushed; PR #2 passed fresh GitHub `Verify`, `Vercel`, and `Vercel Preview Comments`. Preview `https://paintswitch-44fjlzkoy-paint-switch.vercel.app` reached `Ready` and displayed one approved response in real time within the bounded observation described below. The WSS explanation remains an inference, not proven causation.
- **Production merge and widget-opening evidence before D-054 rename — verified 2026-08-08:** PR #2 was squash-merged to `main` as `7fb7359ccd5d1518d2dd4763e34350da9168d0cb`; Vercel Production deployment `paintswitch-ghh6fv5dl-paint-switch.vercel.app` reached `Ready`; and `https://paintswitch.com` returned HTTP `200` with the merged homepage. The page showed the chat prompt, and the widget opened with the then-current `PaintSwitch AI Assistant` title and an AI-disclosing introduction. The launcher count was one on `/` and zero on `/privacy` and `/terms`. A hidden file input existed but was not visible. Zero site/widget console logs remained after unrelated extension logs were excluded, including across the route checks. Live headers retained the expected security baseline, omitted `X-Powered-By`, kept `frame-src 'none'`, and included the documented exact HTTPS widget/service/static origins plus exact `wss://services.leadconnectorhq.com` in `connect-src`. No Production chat message was sent, so Production reply behavior and latency are not verified. The lead-delivery environment was not changed and remains previously verified disabled; ads and promotion remain off.
- **Provider-managed `Switch bot` rename and repository release — verified 2026-08-08:** After owner approval in D-054, the signed-in GoHighLevel bot name, live widget title, widget introduction, and saved first-reply instructions were changed to `Switch bot`. PR #4 was squash-merged to `main` as commit `0dd1c74f2a0d2b5260f36061b573986eb5b57126`; Vercel Production deployment `paintswitch-pidsb5zpf-paint-switch.vercel.app` reached `Ready`. A fresh `https://paintswitch.com` session opened with the `Switch bot` heading and an introduction explicitly identifying it as PaintSwitch's AI assistant. The live `/privacy` route identifies the chat the same way and shows effective date August 8, 2026. No Production chat message was sent during verification. All D-052/D-053 safeguards and the promotion-off boundary remain unchanged.

### Missing, placeholder, or not connected

- The service area has no city, county, ZIP, address-eligibility, or travel-zone logic. The form collects one combined project-location value and the local delivery client maps it to the approved `Project Location` Opportunity field; no native address normalization or service-eligibility decision is implemented.
- `/api/leads` provides a fail-closed server boundary with strict key/type/value validation, practical local-part and DNS-label email validation, a 16 KiB bounded streaming body read, a five-second body-read deadline, exact JSON media-type checking, exact same-origin production-host checks for `paintswitch.com` and `paintswitch-web.vercel.app`, local-development and Vercel-preview handling, generic no-store/nosniff responses, and a 55-second function maximum. It maps delivery conflict to `409` and busy/unavailable states to generic retryable `503` responses. With delivery disabled or configuration incomplete, a valid request remains unavailable and cannot truthfully reach the success state end to end.
- The private integration exists, and its confidential token, location ID, pipeline ID, stage ID, and seven custom-field IDs are configured server-side in Vercel. The token is Sensitive and available to Production and Preview; no value is tracked or documented. `LEAD_DELIVERY_ENABLED` is `false`.
- Commit `efb96b9` contains a native-fetch GoHighLevel client for Contact upsert, Opportunity reconciliation search, and Opportunity creation. It maps Contact Preference plus all six approved Opportunity values without exposing provider credentials to browser code.
- Commit `efb96b9` also contains an Upstash REST state client and delivery coordinator. It uses a token-checked 60-second lock, a SHA-256 payload hash, durable `contacting`, `contacted`, `creating`, `ambiguous`, `rejected`, and `completed` phases, streamed provider-response caps, ambiguous-result reconciliation, and explicit rejected-create recovery after configuration correction. It records sanitized delivery events with the submission UUID rather than raw lead fields or credentials; the broader privacy classification of technical and provider identifiers remains unresolved.
- These safeguards support at-most-one automatic Opportunity creation plus reconciliation; they do not establish a strict exactly-once guarantee across independent providers. The owner accepted the Upstash marketplace terms, and the database and Vercel secrets exist. D-030 confirms a 30-day TTL for Upstash technical idempotency/delivery-state records and a maximum of five beta lead submissions per client IP per ten minutes. Current source implements both safeguards and local automated tests pass. One earlier controlled protected-Preview submission verified a completed state containing a version, payload hash, provider IDs, completion phase, and updated timestamp but no raw submitted name, email, phone, location, service, description, or preference fields; 2,591,535 seconds of TTL remained, the completion lock was absent, and one Preview rate counter had value `1` with 101 seconds remaining. That earlier run did not test the threshold; the 2026-08-05 final-candidate rehearsal recorded below verified the five-attempt cap and following `429`. Concurrent requests, broader failure/retry behavior, actual expiration, and Production operation remain unverified.
- Browser-side source/campaign capture and mappings to `Campaign Source` and `Campaign Name` exist. One controlled protected-Preview submission verified both UTM values in GoHighLevel; consent behavior and Production-persisted conversion attribution remain unverified.
- The owner approved the D-037 through D-045 beta privacy, data-use, retention, privacy-request, contact, SMS-off, age, Terms, and response-operation rules on 2026-08-04. Production now publishes those customer-facing disclosures at `/privacy`, `/terms`, and beside the form. D-053 dated 2026-08-06 extends the up-to-12-month unconverted-lead schedule to chat messages and requires manual deletion/anonymization because HighLevel fixed active-account retention is unverified. The exact synthetic conversation `Guest Visitor Svdtu`, containing no email, phone, or customer data, was permanently deleted through HighLevel's `Delete Conversation` > `Delete Forever` dialog, and the inbox afterward showed no unread conversations. This passes one manual synthetic chat-deletion rehearsal only. Professional review, provider-enforced retention, anonymization, backup deletion, and the ongoing operating procedure remain unverified. `BETA_OPERATIONS_RUNBOOK.md` documents the earlier lead-record procedure. No automated CRM/chat retention workflow or verified human-response operation exists. D-035 confirms only the internal/legal Virginia DBA direction; the repository provides no verified filing, exact registrant, registered-address, tax/EIN, licensing, authorized-representative, or A2P-acceptance facts.
- The form's Preview success/recovery paths, CRM mappings, immediate hello receipt, both approved stages, both Saved/published five-minute branches, no-customer-action behavior, and routed Alex receipt are externally verified. Mobile Outlook shows the Alex message at 4:23 PM, corroborated by Email Analytics Delivered. Broader failure/concurrency, Production operation, operational activation, and human-response timing remain unverified.
- No instant quote flow, room input flow, room-size calculation, eligibility engine, price engine, checkout, booking, scheduling, deposit, or payment behavior is implemented.
- The confirmed flat $125 fuel/materials adjustment is not implemented because no pricing engine or project-price workflow exists.
- No photo upload or review workflow is implemented.
- A committed and hosted-Preview-verified chatbot UI exists. The earlier `649f06d` Preview loaded the widget only on the homepage and displayed two backend replies after reload; a third correct ZIP-availability refusal did not display during a 30-second no-reload observation, so real-time delivery/latency was unaccepted in that earlier Preview. Commit `85117a1` and its Ready Preview later displayed one approved response within a 20-second no-reload observation. Exact two-second timing and broader real-time reliability remain unverified. The HighLevel bot was changed from Off to Auto Pilot solely for controlled owner testing and remains Live Chat-only. One manual synthetic conversation deletion rehearsal passed. Production, mobile viewport, provider-enforced retention/anonymization, and both shutdown kill-switch paths remain unverified. There is no PaintSwitch chatbot API, model client, transcript database, summary/export workflow, automatic Contact/Opportunity creation, automatic context transfer, booking, payment, or chatbot action in repository source.
- No custom consultation intake workflow is implemented.
- No automated follow-up, enabled Production CRM delivery, production-persisted conversion attribution, analytics integration, or human-handoff workflow is implemented.
- The pushed Preview candidate contains GoHighLevel/Upstash clients and server-side environment-variable references but no added provider dependency. Its provisioned Upstash and GoHighLevel path was exercised once with synthetic data in an access-protected Preview, then disabled again. This is controlled Preview evidence, not Production or launch acceptance.
- No proposal-generation workflow or operational first-project readiness evidence is present.
- No verified marketing traffic or measurable-lead workflow is implemented.
- The current rendered brand mark is a text-only wordmark approved by D-058 only as the interim lead-generation beta treatment; it is not a final production logo using the owner-preferred paint-roller-with-paint-behind direction. Final logo artwork and variants, post-beta/final typography or token changes beyond the approved candidate, and an exact warm-gold value remain TBD.
- No authentic, permissioned PaintSwitch before/after customer-project asset or completed story is present. The local generated hero is explicitly an illustrative color study and must not be represented as customer work. Final project selections, facts, captions, rights evidence, and supporting homepage/funnel copy remain TBD.
- One fail-closed-by-configuration API route is present. Current source has external API clients, durable-state logic, a provisioned Upstash connection, and the D-030 safeguards. A protected Preview temporarily enabled delivery and verified one website-to-Upstash-to-GoHighLevel-to-workflow-action path. The Preview environment setting and latest branch Preview are now disabled, Production remains disabled, and the prior immutable enabled Preview remains access-protected. The application has no user authentication, and complete production notification operation remains unverified.
- Sixty-one automated tests passed locally for commit `85117a1`, including the prior lead-delivery, validation, rate-limit, legal, market-copy, privacy, widget, and exact non-wildcard WSS-origin contracts. Lint and TypeScript type checking passed. The 2026-08-08 Next.js `16.3.0` chat-enabled production build compiled successfully and reported `/`, `/_not-found`, `/api/leads`, `/privacy`, and `/terms`. PR #2 then passed all three fresh checks for the pushed commit.
- `next.config.ts` disables the framework-identifying response header and sets the static-page security baseline. When `HIGHLEVEL_CHAT_WIDGET_ENABLED` is not `true`, the prior self-only CSP remains. Commit `85117a1` retains the three exact HTTPS LeadConnector origins and adds only exact `wss://services.leadconnectorhq.com` to chat-enabled `connect-src`; `frame-src` remains `'none'`. Preview passed one bounded real-time display check, and Production passed live CSP, homepage-only placement, widget-opening, and no-site/widget-log checks. No Production message was sent, and both disabled-state checks remain unverified.
- No tracked Vercel project configuration or other deployment configuration is present. `.vercel` is ignored.

### Current documentation/code conflicts and implementation gaps

- The merged Production application aligns with the approved four-service taxonomy, “Request a Quote” action, DMV-wide individual review with Virginia priority, beta exclusions, placeholder-review removal, approved customer-facing policy package, D-053 chatbot boundary, and D-054 `Switch bot` identity. PR #2 was squash-merged to `main` as `7fb7359ccd5d1518d2dd4763e34350da9168d0cb`; PR #4 later merged the matching Privacy identity/date as `0dd1c74f2a0d2b5260f36061b573986eb5b57126`. Both resulting Production deployments reached `Ready`. The live apex opens the homepage-only widget as `Switch bot` with explicit AI disclosure, and the live Privacy route matches that identity. D-055 through D-058 are reflected in a locally verified, owner-approved but uncommitted editorial beta candidate. Preview and Production verification for that redesign remain pending; existing Production evidence is not evidence for the redesign, and D-058 is not merge, deployment, or promoted-traffic approval.
- Decision D-023 requires both a custom branded PaintSwitch form and a secure server-side GoHighLevel connection. The frontend, server boundary, private integration, server-only Vercel configuration, GHL client/mappings, durable state design, D-030 safeguards, tests, pushed branch, Preview deployments, Upstash database, and Sensitive Upstash variables exist. Controlled protected-Preview evidence now verifies the primary website-to-Upstash-to-GoHighLevel-to-workflow path, one fail-closed-to-success retry using the same retained synthetic submission, at-most-one provider result for that retry, the five-request hosted rate-limit threshold, and a blocked additional valid request. Broader failure paths, concurrency, Production operation, and full launch acceptance remain unverified.
- The required immediate on-page confirmation was observed after a successful controlled protected-Preview delivery with the exact copy, `Your request was sent. A PaintSwitch team member will review the details and follow up.` This does not prove the Production path or human follow-up. D-042 now requires automated customer SMS to remain off for the beta and makes `Text` contact preference insufficient as SMS consent. No SMS/customer action ran during the Preview test. Any later activation remains governed by D-027's separate optional unchecked opt-in and still-unresolved operational/legal work.
- Project location and attribution are mapped to the four D-026 Opportunity fields. D-032's workflow remains Saved/published. Preview paths, immediate hello receipt, rate threshold, D-049 stages/branches, post-configuration row-level hello/Alex delivery, routed Alex receipt, live SPF/DMARC/MX lookup, and dedicated sending-domain configuration are verified. The Alex action reuses D-032-approved wording; no new copy was invented. Native address normalization, owner-user assignment, transition acknowledgment, broader failure/concurrency, domain-warmup completion, raw-header authentication evidence, professional/legal review, privacy/retention operations, human response, and Production verification remain unresolved or incomplete.
- A customer-facing chatbot is **Confirmed**. D-051/D-052 approve GoHighLevel Conversation AI Prompt Based/Start from Scratch, OpenAI GPT-4.1, and the bounded prompt. D-053 additionally approves the homepage-only interface, exact disabled features/channels/actions, reply settings, handoff, summary/export-off state, manual retention boundary, and dual kill switches. D-054 confirms the `Switch bot` name, and D-059 makes it part of the intended next-weekend full-design launch. Commit `85117a1`, fresh passed PR checks, and its hosted Preview verify one bounded real-time desktop response; earlier `649f06d` evidence and one manual synthetic deletion rehearsal remain valid. The merged Production homepage verifies widget presence, homepage-only placement, opening, and AI disclosure; a later live check verifies the `Switch bot` title and introduction. Exact reply delay/cap/sleep runtime, broad safety, mobile/accessibility, both shutdown paths, Production response/latency, and provider-enforced retention/anonymization remain unresolved.
- GoHighLevel and its intended functions are **Confirmed**. The sub-account, pipeline/stage, fields, integration, Vercel/Upstash configuration, corrected received-test evidence, published owner-notification workflow, controlled manual and protected-Preview executions, post-configuration row-level hello/Alex delivery, routed Alex receipt, live SPF/DMARC/MX lookup, dedicated PaintSwitch sending-domain configuration, and Conversation AI configuration are externally verified. The bot is now Auto Pilot for the controlled D-053 owner test and remains Live Chat-only. The 2026-08-05 retained-submission rehearsal recovered from fail-closed delivery exactly once, producing one Contact, one Opportunity, one workflow enrollment, and a `Success` owner-notification action. The hosted rate counter capped at five, and one additional valid request returned `429` without creating a duplicate provider record. Production widget presence/opening is verified, but enabled Production lead delivery, Production chatbot response/latency, notification-failure recovery, domain-warmup completion, raw-header authentication evidence, broader failure/concurrency testing, human-response operation, and a phone number remain absent or unresolved.
- The lead-generation beta is now the confirmed first release; the seven end-state outcomes are not all implemented and post-beta sequencing remains **TBD**.
- Website copy remains implementation content and is not evidence that any unrecorded policy is approved.

## Historical reports

### Current local application verification — 2026-08-03

- **Verified:** Fifty-one automated tests plus lint, TypeScript type checking, and the Next.js production build passed after the current D-030 application changes.
- **Verified:** The production build used Next.js `16.2.12` and reported static routes `/` and `/_not-found` plus dynamic route `/api/leads`.
- **Verified:** A built-app HTTP smoke test returned `200` for `/` and the intentional fail-closed `503` for a structurally valid `/api/leads` request; it did not claim delivery.
- **Verified:** The frontend was inspected locally at 1440 × 900 and 390 × 844 with no horizontal overflow. The native mobile menu opened, navigated to Services, and closed. A valid local form attempt reached the intentional unavailable response, displayed accurate failure copy, and preserved all entered values.
- **Verified:** Regression tests prove that an exact same-origin request from `https://paintswitch-web.vercel.app` reaches the intentional fail-closed `503` rather than being rejected as cross-origin, while a mismatched origin remains rejected. Direct API submissions using malformed or hostile email forms are rejected.
- **Verified:** A built production server returned `200` for `/`, the intentional `503 LEAD_DELIVERY_UNAVAILABLE` for a valid local `/api/leads` request, the configured CSP/frame/nosniff/referrer/permissions headers, and no `X-Powered-By` value.
- **Verified:** Built-site Chrome QA proved that invalid phone input receives a field-specific error and Phone focus, empty submission focuses Name and exposes all seven field errors, the skip link focuses `MAIN#main-content`, a valid unavailable submission retains all seven values, and the 404 page renders with the security headers. No CSP violation appeared during these flows.
- **Boundary:** The parser/route/browser checks verify local request handling and fail-closed behavior. The delivery tests verify mapping and failure/recovery contracts against in-memory fake Upstash and GoHighLevel responses; provisioning the database alone did not verify runtime behavior, and the controlled manual Opportunity verified only one external CRM-triggered workflow execution. The separate protected Preview evidence below verifies one real website-to-Upstash-to-GoHighLevel-to-workflow-to-inbox success path. It does not verify approved-policy implementation, failure/retry/concurrency behavior, Production deployment, human-response timing, or launch acceptance.

### Earlier local verification report — 2026-07-30

- **Status:** Historical/Needs verification
- Lint and the production build were reported as passing on Codyx on 2026-07-30.
- The reported build used Next.js `16.2.10` and listed static routes `/` and `/_not-found`.
- This earlier report is preserved as history. A newer frontend-only verification passed on 2026-08-02, but final-candidate checks must run again after the lead-delivery path and remaining launch changes are complete.

### Dependency-install report — 2026-07-30

- **Status:** Historical/Needs verification
- `npm ci` reportedly identified 12 high-severity vulnerabilities.
- That historical count is superseded for current planning by the verified 2026-08-02 review below; it remains preserved as history.
- Do not recommend or run `npm audit fix --force` automatically. Review the advisories, affected dependency paths, available compatible updates, and application impact before proposing remediation.

### Current dependency-security review — 2026-08-04

- **Verified:** A current audit initially reported three high-severity production package entries under Next.js `16.2.10`, including direct Next.js advisories plus transitive PostCSS and Sharp findings.
- **Verified intermediate remediation:** Next.js and `eslint-config-next` were first updated to the signed patch release `16.2.12`. The direct Next.js advisories cleared. At that intermediate stage, fifty-seven tests, lint, type checking, and a production build reporting `/`, `/privacy`, `/terms`, `/_not-found`, and `/api/leads` passed locally. D-047 and the current verification below supersede this as the active package/test result.
- **Verified remediation:** Development-only `brace-expansion` findings were resolved by compatible transitive patch updates to `1.1.18` and `5.0.9`; no force fix was run.
- **Resolved current audit — 2026-08-04:** Under D-047, the owner approved and the repository installed exact stable `next@16.3.0` and `eslint-config-next@16.3.0` without changing React, using a Preview/Canary package, or running a force fix. The resolved dependency graph includes PostCSS `8.5.23` and optional Sharp `0.35.3`.
- **Verified result:** `npm audit --omit=dev --json` reports zero vulnerabilities across production dependencies. Fifty-nine tests, lint, TypeScript type checking, and the Next.js `16.3.0` production build pass locally. The build reports `/`, `/privacy`, `/terms`, `/_not-found`, and `/api/leads`.
- **Verified local production smoke:** `/`, `/privacy`, and `/terms` each return `200`; CSP and `X-Frame-Options: DENY` are present; `X-Powered-By` is absent. A valid form submission while delivery is disabled shows the approved recoverable failure and retains all entered values. The built output contains no Jen Contracting, private-address, or MHIC content and now explicitly preserves DMV intake with Virginia priority.
- **Prior exposure assessment:** Before D-047, the PostCSS findings required attacker-controlled CSS processing and the Sharp finding required untrusted image decoding; repository inspection found neither path. D-028 preserves that historical disposition but is no longer the active basis for shipping the three now-remediated findings.
- Primary references: [Next.js 16.2.12 signed release](https://github.com/vercel/next.js/releases/tag/v16.2.12), [PostCSS arbitrary-file-read advisory](https://github.com/advisories/GHSA-6g55-p6wh-862q), [PostCSS path-traversal advisory](https://github.com/advisories/GHSA-r28c-9q8g-f849), and [Sharp/libvips advisory](https://github.com/advisories/GHSA-f88m-g3jw-g9cj).

### Deployment report and current verification

- **Historical/Needs verification — 2026-07-24:** A deployment was reported at [https://paintswitch-web.vercel.app](https://paintswitch-web.vercel.app), together with GitHub-to-Vercel automatic deployment.
- **Verified — 2026-08-01:** A read-only HTTP health check returned `200 OK` from Vercel for the reported URL.
- **Verified — 2026-08-02:** The signed-in Vercel project `paint-switch/paintswitch-web` was inspected. It is linked to `https://github.com/paintswitch/paintswitch-web`, and the displayed production source was branch `main` at commit `dc0c8a6`.
- **Verified — 2026-08-03:** Vercel showed `paintswitch.com`, `www.paintswitch.com`, and the Vercel production domain with Valid Configuration. The apex is assigned to Production, and `www` is configured as a `308` redirect to the apex. Public HTTPS checks returned `200` for `https://paintswitch.com` and `308` from `https://www.paintswitch.com` to `https://paintswitch.com/`.
- **Verified branch-to-Preview deployment — 2026-08-03:** Commit `efb96b9` (`Build lead-generation beta`) was pushed on `codex/lead-generation-beta`. Vercel automatically created the Ready Preview at [https://paintswitch-m6o5sbszy-paint-switch.vercel.app](https://paintswitch-m6o5sbszy-paint-switch.vercel.app) in 21 seconds. The public GitHub commit page showed `2 / 2` Vercel-related checks: `Vercel — Deployment has completed` and `Vercel Preview Comments`. These checks prove successful Preview automation, not the repository's application-verification workflow or production release.
- **Verified hosted Preview behavior — 2026-08-03:** The Preview DOM, title, and branded form structure were inspected. A structurally valid hosted submission returned the generic fail-closed error because `LEAD_DELIVERY_ENABLED=false`; no GoHighLevel delivery was attempted. This verifies deployed fail-closed behavior, not real-provider or end-to-end acceptance.
- **Verified controlled enabled Preview — 2026-08-04:** Commit `3bac3ba` was exercised behind Vercel Standard Protection with Require Log In. Preview-only `LEAD_DELIVERY_ENABLED=true` was applied temporarily while Production remained explicitly `false`. Deployment `CrJA8ve4qzNHneVLwEtkr5N15gJu` at `paintswitch-pp6wvxmih-paint-switch.vercel.app` was Ready at 1:18:57 PM EDT. One synthetic browser submission ran from 1:19:53 PM to 1:19:57 PM and displayed `Your request was sent. A PaintSwitch team member will review the details and follow up.` This was controlled Preview evidence, not Production or public launch acceptance.
- **Verified fail-closed restoration — 2026-08-04:** The Preview environment setting and latest branch Preview were returned to `false`; fresh deployment `6NrFmAef7vgP3dm9iuC11DBYEJ1d` at `paintswitch-7qbi2cue9-paint-switch.vercel.app` was Ready at 1:26:34 PM EDT. Production remained `false`. The immutable enabled-deployment URL remains access-protected.
- **Verified production contradiction — 2026-08-03:** The apex still serves the prior `main` build, not commit `efb96b9`. It contains standalone Drywall Repair, three explicitly labeled placeholder testimonials, nonfunctional `#` Privacy/Terms links, estimate/contact CTAs that do not reach a working form, and universal-consultation copy. These are implementation conflicts, not approved policy. The corrected candidate is available only as a Preview.
- **Verified hosting-plan resolution — 2026-08-04:** The owner approved Vercel Pro for commercial beta hosting under D-033. A signed-in Vercel dashboard check then showed the `Paint Switch` team labeled `Pro` and the `paintswitch-web` project within that team. This resolves the prior Hobby-plan choice blocker. Invoice/payment administration was not inspected; Production promotion, rollback, monitoring, and final deployment QA remain unverified.
- **Verified — 2026-08-02:** GoDaddy DNS for `paintswitch.com` uses an apex `A` record pointing to Vercel's displayed value `216.198.79.1`. The existing `www` CNAME points to `paintswitch.com`; nameservers and unrelated DNS records were left unchanged.
- **Partially verified — 2026-08-04:** Sensitive GHL configuration and five Sensitive Upstash variables are present in Vercel for Production and Preview. After one controlled Preview-only enabled test, `LEAD_DELIVERY_ENABLED` was verified `false` again for Preview and remained `false` for Production. GitHub-branch-to-Vercel-Preview automatic deployment is verified; production promotion/release behavior, other environment settings, rollback procedure, monitoring, alerting, and production-change rehearsal remain unverified.
- **Verified final-candidate Preview rehearsal — 2026-08-05:** Application commit `beb6781` passed the GitHub Actions `Verify` workflow and its Vercel Preview reached `Ready`. With Preview delivery disabled, one synthetic submission failed closed. After delivery was temporarily enabled for Preview only and the candidate redeployed, retrying that same unchanged retained submission recovered exactly once: GoHighLevel contained one matching Contact, one matching Opportunity, one workflow enrollment, and a `Success` internal-notification action. In a separate delivery-disabled threshold check, the hosted rate counter capped at five counted valid attempts; those attempts returned the expected delivery-disabled `503`, and one additional valid attempt returned `429` without creating a duplicate Contact, Opportunity, or workflow enrollment. No synthetic personal data, provider identifiers, or Redis keys are recorded here.
- **Verified fail-closed restoration — 2026-08-05:** `LEAD_DELIVERY_ENABLED` was restored to `false` for Preview after the rehearsal, and the resulting disabled redeployment reached `Ready`. Production remained disabled and was not used for this test.
- The repository itself does not contain tracked Vercel configuration proving the external deployment connection.

### GoHighLevel and server-configuration verification — 2026-08-03

- **Verified:** A signed-in GoHighLevel agency account and a Paint Switch sub-account exist and were accessible.
- **Verified:** The sub-account contains a `Marketing Pipeline`, example opportunities, contacts, tasks, and revenue values that appear to be HighLevel sample/demo content. They are not evidence of a production PaintSwitch lead workflow or business activity.
- **Verified:** GoHighLevel **My Staff** showed no location users, so no production lead owner can yet be assigned or notified through a verified location-user record.
- **Verified current provider state — 2026-08-05:** `PaintSwitch Lead Intake` contains initial stage `New Quote Request` and second stage `Contact Attempted`. The prior exactly-one-stage implementation is superseded.
- **Verified current escalation configuration — 2026-08-05:** The Saved/published `PaintSwitch Website Lead - Owner Notification` workflow runs Opportunity Created, the existing immediate `Email Owner - hello@paintswitch.com`, an exact five-minute Wait, and a condition. `Still New` runs internal email `Escalate to Alex - still New Quote Request` to `alex@paintswitch.com`; `Contact Attempted` ends without an Alex action. No SMS/customer action exists. The Alex action reuses the exact D-032-approved hello subject/body, an implementation fact rather than new copy approval.
- **Confirmed owner direction — 2026-08-05:** Add `Contact Attempted` as the second internal stage. Keep the immediate every-lead hello alert. After five minutes, alert Alex only if the Opportunity still remains in `New Quote Request`; if it has moved to `Contact Attempted`, do not alert Alex. Automated customer SMS remains off.
- **Verified branch executions — 2026-08-05:** Unhandled enrolled at 4:18:14; hello executed; Wait finished at 4:23:15; `Still New` executed at 4:23:16; Alex Event status was `Success` at 4:23:16; workflow finished at 4:23:18. Handled enrolled at 4:20:40, moved to `Contact Attempted` before cutoff, recorded hello Event status `Success` at 4:20:41, finished the Wait at 4:25:41, executed `Contact Attempted` at 4:25:42, and finished at 4:25:43 with no Alex action in the execution log. Routed Alex mailbox receipt is not verified by Event status `Success`.
- **Verified routed receipt — 2026-08-05:** The owner's 4:33 PM ET mobile Outlook screenshot shows sender `PaintSwitch`, subject `New PaintSwitch website lead`, received at 4:23 PM in the Alex inbox. Email Analytics records Alex Delivered at 4:23 PM and hello Delivered at 4:18 PM and 4:20 PM.
- **Verified delivery-status discrepancy — 2026-08-05:** Email Analytics also records one earlier `hello@paintswitch.com` message as `Failed` at 11:38 AM. That time is near the retained-submission workflow action previously recorded as `Success`, but exact linkage is unverified. Workflow execution status therefore does not prove receipt; root cause, the authentication state of that earlier message, retry/fallback behavior, operator failure handling, and Production delivery remain unverified.
- **Remaining / unverified:** Exact transition ownership and acknowledgment procedure, any future distinct Alex wording, failure/retry behavior, operational activation, and human-response rehearsal remain **TBD** or unverified.
- **Verified:** `Service Type` was created as an Opportunity single-select dropdown with Interior, Exterior, Cabinet, and Commercial in the `Opportunity Details` folder.
- **Verified:** `Project Description` was created as an Opportunity multi-line field in the `Opportunity Details` folder.
- **Verified:** `Contact Preference` was created as a Contact single-select dropdown with Call, Text, and Email in the `General Info` folder.
- **Verified:** `Project Location`, `Website Submission ID`, `Campaign Source`, and `Campaign Name` were created as the four D-026 Opportunity fields. Their identifiers and the three D-022 field identifiers are configured in the Vercel server-side field mapping.
- **Verified:** The phone system has no phone number, and A2P messaging registration has not been started. D-035 confirms the internal/legal direction that PaintSwitch is a Virginia DBA of Jen Contracting, but no filing, exact formal registrant, registered-address, tax/EIN, licensing, authorized-representative, or A2P-acceptance evidence is recorded. Automated customer SMS is off for the beta under D-042 and remains not launch-ready for any later activation.
- **Verified dedicated sending-domain configuration — 2026-08-05:** `mg.paintswitch.com` was configured in GoHighLevel through GoDaddy. Domain Connect created SPF, DKIM, CNAME, and MX records; `_dmarc.mg` was added manually with `v=DMARC1;p=none;`. Live DNS later returned Mailgun MX records, SPF authorizing LeadConnector and Mailgun, and DMARC monitoring policy. GoHighLevel displayed the domain enabled, Warmup In Progress at Stage 1, later at 0.1% with the counter reading “1 of 1,000 emails today,” Shared IP Active, and SSL Issued. This verifies configuration and public DNS state, not warmup completion, raw-header SPF/DKIM/DMARC results, the earlier failed row's cause, notification failure recovery, or Production deliverability.
- **Verified standalone profile correction and remaining legal gap — 2026-08-05:** GoHighLevel Business Profile now uses Friendly Business Name `PaintSwitch`, Business Email `hello@paintswitch.com`, and Business Website `https://paintswitch.com`; the prior Jen Contracting-domain business email is no longer present. Legal Business Name and authorized-representative fields remain incomplete. D-035 resolves the internal/legal structure direction but does not verify that the DBA is filed or supply the exact registrant, address, tax, licensing, or representative facts. Required legal-disclosure treatment remains unresolved. D-001 prohibits any customer-facing Jen connection unless the owner and legal review resolve a mandatory-disclosure conflict.
- **Verified external subscription state — 2026-08-06:** After explicit owner approval, the HighLevel Starter subscription was activated at $97 per month. The 30-day AI Employee Unlimited free trial was then started with authorization to renew at an additional $97 per month after the trial. HighLevel displayed `Your subscription has been activated` for Starter and `Subscription enabled successfully` plus Current plan `Unlimited` for AI Employee. The exact first AI renewal date, taxes, and cancellation procedure were not displayed. The subscription bundle does not approve unrelated AI, voice, messaging, review, content, image, payment, or other modules for customer-facing use.
- **Verified external Conversation AI configuration and bounded tests — 2026-08-06 through 2026-08-07:** The bot then named `PaintSwitch AI Assistant` existed in the verified PaintSwitch location as a Prompt Based bot started from scratch. The owner approved `OpenAI GPT 4.1` and a limited safety-first prompt, and that prompt was saved. Four controlled provider-panel tests passed the bounded behaviors exercised. D-053's exact Live Chat widget/settings were also saved: Live Chat only, prohibited collection/channels/actions off, 10 replies, two-second delay, images/voice responses off, manual/workflow sleep, direct form/mailbox handoff, Conversation Summary off, and no transcript-export workflow. On 2026-08-07 the assistant was changed from Off to Auto Pilot for controlled owner testing. The hosted Preview showed no pre-chat contact form and no visible attachment/voice control; a hidden `display:none` file input remained in provider markup, so provider-internal attachment code is not claimed absent. Voice AI remains disabled.
- **Verified hosted chatbot Preview and synthetic deletion rehearsal — 2026-08-07:** Commit `649f06d` was pushed on `codex/lead-generation-beta`, and PR #2 passed GitHub `Verify`, `Vercel`, and `Vercel Preview Comments`. Preview `https://paintswitch-9vzqs7zmi-paint-switch.vercel.app` loaded the widget only on the homepage. Its visible title and intro disclosed AI. Two synthetic messages passed the observed boundaries and appeared after page reload: the services response named interior, exterior, cabinet, and commercial and directed to `Request a Quote`; the pricing/booking request was refused and directed to the form/human follow-up. A third message asked whether ZIP `22102` could be confirmed. HighLevel generated a response refusing to guarantee availability and routing to `Request a Quote`/human review, but the browser widget did not display it during a 30-second no-reload observation. Real-time delivery/latency was therefore unaccepted in that earlier Preview. Browser logs contained only unrelated Chrome-extension warnings and no site/widget error. The exact synthetic `Guest Visitor Svdtu` conversation contained no email, phone, or customer data and was permanently deleted through `Delete Conversation` > `Delete Forever`; the inbox then showed no unread conversations. This passes one manual synthetic deletion rehearsal, not provider-enforced retention or anonymization. Mobile viewport, both kill-switch shutdown tests, Production runtime, provider-enforced retention/anonymization, and exact real-time latency remain unverified.
- **Verified WSS hosted retest — 2026-08-08:** Commit `85117a1` was pushed, PR #2 passed all three fresh checks, and Preview `https://paintswitch-44fjlzkoy-paint-switch.vercel.app` reached `Ready`. In a fresh subdomain/session, the homepage widget opened and synthetic `What painting services do you offer?` received the approved AI-disclosed interior/exterior/cabinet/commercial response with `Request a Quote` direction within the 20-second no-reload observation. Browser diagnostics contained zero site/widget logs after excluding unrelated extension logs. This passes real-time browser display for one bounded case after the exact WSS allowance; it does not prove the exact two-second delay, cap/sleep behavior, broad safety, mobile/accessibility, either shutdown path, Production runtime, or promoted traffic.
- **Verified Production deployment and widget opening before D-054 rename — 2026-08-08:** PR #2 was squash-merged to `main` as commit `7fb7359ccd5d1518d2dd4763e34350da9168d0cb`. Production deployment `paintswitch-ghh6fv5dl-paint-switch.vercel.app` reached `Ready`, and `https://paintswitch.com` returned `200` and loaded the merged homepage. The chat prompt appeared, and the widget opened with the then-current title `PaintSwitch AI Assistant` and an AI-disclosing introduction. The launcher count was one on `/` and zero on `/privacy` and `/terms`; a hidden file input existed but was not visible. Diagnostics showed zero non-extension site/widget console logs across the route checks. The live CSP included exact `https://widgets.leadconnectorhq.com`, `https://services.leadconnectorhq.com`, `https://stcdn.leadconnectorhq.com`, and `wss://services.leadconnectorhq.com`, with `frame-src 'none'`; expected security headers were present and `X-Powered-By` was absent. No Production chat message was sent, so this does not verify a Production reply or latency. Lead delivery was not changed from its previously verified disabled state, and ads/promotion remain off. Production response, mobile/accessibility, exact delay/cap/sleep, both kill switches, provider retention/anonymization, and promoted release remain open.
- **Verified:** The original `Marketing Pipeline` and visible sample/demo data were left untouched while the dedicated pipeline was added.
- **Verified:** The `PaintSwitch Website Lead Intake` private integration exists with the required beta permissions for contact writes, Opportunity reads/writes, and custom-field reads. Its token is stored as a Sensitive Vercel environment variable for Production and Preview. Location, pipeline, and stage identifiers are also configured; no confidential value is recorded in documentation.
- **Verified:** The owner explicitly accepted the Upstash marketplace terms. The free database `paintswitch-lead-idempotency` was created in Washington, D.C. (`iad1`) with eviction disabled and a displayed allowance of 500,000 commands per month. It is connected to the Vercel project `paintswitch-web` through five Sensitive Production-and-Preview variables: `UPSTASH_REDIS_REST_KV_REST_API_URL`, `UPSTASH_REDIS_REST_KV_REST_API_TOKEN`, `UPSTASH_REDIS_REST_KV_REST_API_READ_ONLY_TOKEN`, `UPSTASH_REDIS_REST_REDIS_URL`, and `UPSTASH_REDIS_REST_KV_URL`. No value is recorded in documentation.
- **Verified current gate — 2026-08-05:** The Preview environment setting and latest branch Preview have `LEAD_DELIVERY_ENABLED=false` after the final-candidate rehearsal, and the disabled redeployment reached `Ready`. Production remains `false`. Delivery was enabled only for the protected Preview long enough to exercise the retained-submission recovery and rate-limit paths, then restored to fail closed.
- **Verified in current source and controlled hosted paths — 2026-08-05:** Shared Upstash configuration validation serves both delivery state and rate limiting. Every guarded technical-state write receives the 30-day TTL. An atomic fixed-window limiter runs after schema validation and before GoHighLevel delivery, allowing five valid submissions per HMAC-pseudonymized client-IP bucket per 600 seconds. Hosted evidence now verifies that the same unchanged synthetic submission can first fail closed and then recover exactly once after Preview delivery is enabled, without a duplicate Contact, Opportunity, or workflow enrollment. It also verifies a counter capped at five and an additional valid request returning `429` without duplicate provider delivery. Broader concurrent-request behavior, other failure modes, actual expiration, and Production behavior remain unverified.
- **Verified received-message evidence — 2026-08-04:** Owner-supplied screenshot evidence shows the corrected controlled test reached `hello@paintswitch.com`; visible sender name is `PaintSwitch`, routing address is `hello+paintswitch.com@mail.msgsndrroute.com`, no Jen identity appears, and the subject/body match the approved internal alert. AC-044 is satisfied. This screenshot predates the later dedicated-domain setup and does not prove raw-header authentication or workflow-trigger operation; the separate controlled trigger evidence below verifies one manual execution only.
- **Verified published configuration and controlled trigger — 2026-08-04:** Before publication, `PaintSwitch Website Lead - Owner Notification` was verified with Opportunity Created filtered to `PaintSwitch Lead Intake`, recipient `hello@paintswitch.com`, From name `PaintSwitch`, From email `hello@paintswitch.com`, and the approved subject/body. After the owner said “Publish work flow,” the publish switch was enabled, the global workflow was saved, and reload showed it still Saved with the publish switch checked. With separate approval, an isolated synthetic Contact named `TEST PaintSwitch Workflow 2026-08-04` and a manual Opportunity named `TEST — PaintSwitch Workflow Verification 2026-08-04` were created in `PaintSwitch Lead Intake` / `New Quote Request`. The Opportunity Created trigger enrolled exactly once at 7:45:28 AM EDT, the `Email Owner - hello@paintswitch.com` internal-notification event reported `Success`, and the workflow finished at 7:45:30 AM. No production website change occurred, and no real customer data was used. This manual execution alone did not verify website delivery. The separate protected-Preview evidence below verifies one website success path and owner-visible receipt; failure recovery, conditional-backup operation, authentication, and Production end-to-end delivery remain unverified.
- **Verified controlled website-driven provider and inbox path — 2026-08-04:** The single protected-Preview submission used submission ID `a0c9e9cd-4e98-4f45-8c1f-16c9a31256ec` and created Contact `w1tEl1ktbKestabbkBr3` plus Opportunity `11u1dIja7oMJXS4srkPu`. All expected Contact and Opportunity mappings and both UTM values were observed. The workflow enrolled exactly once at 1:19:56 PM; execution `01KZ6WM16S7WTFPM40NTEA9381` reported the internal-notification event `Success` at 1:19:57 PM and finished. No SMS or customer-facing workflow action ran. The owner confirmed that the resulting internal message reached `hello@paintswitch.com` at approximately 1:20 PM EDT. This does not verify failure recovery, human-response timing, conditional-backup operation, or Production delivery.
- **Verified / Needs production-safe recheck:** Contact Deduplication appeared configured with Allow Duplicate Contact off and priority Email then Phone. Opportunities settings did not expose an Allow Duplicate Opportunity setting. Contact UI evidence must be reverified during the production-safe test and does not prove duplicate-Opportunity behavior.
- **Historical setup verification — 2026-08-02:** The signed-in GoDaddy Email & Office dashboard showed two unused Email Essentials accounts. The owner accepted the Microsoft Customer Agreement. The full account-creation flow was explicitly scoped to `paintswitch.com`, with username `hello`, display name `PaintSwitch Team`, standalone organization selection `Do not share`, administrator permission required for the first PaintSwitch-domain account, and the existing owner notification address retained. That setup attempt initially failed. On 2026-08-03 the owner reported the mailbox working, and owner-supplied photographic evidence confirmed receipt of the separate test email. Complete outbound, recovery, sender-authentication, and workflow acceptance remain unverified.
- **Verified mailbox-account administration — 2026-08-05:** GoDaddy administration showed both accounts. Direct Alex access, GoHighLevel routing, and routed Alex receipt are separately verified. Human-response operation remains unverified.
- **Verified Alex mailbox access, routing execution, and routed receipt — 2026-08-05:** Microsoft MFA, direct Outlook access, and a 2:24 PM ET controlled self-addressed message verify basic Alex mailbox operation. The GoHighLevel unhandled execution recorded the Alex email action as `Success`, and the handled execution suppressed it as required. Those execution facts alone did not prove receipt; the later owner-supplied Outlook screenshot and GoHighLevel Email Analytics independently verify routed delivery to Alex at 4:23 PM. Human response and complete operational-backup readiness remain unverified.
- **Verified post-configuration workflow exercise — 2026-08-05:** A synthetic Opportunity containing no real customer data triggered the hello internal-notification action at 7:59:28 PM ET, and its execution detail reported `Success`. It was moved to `Contact Attempted`; after the exact Wait finished at 8:04:28 PM, the workflow executed `Contact Attempted` and finished at 8:04:29 PM with no Alex action. That stage transition is not evidence that a human contacted a lead. The selected Email Analytics period displayed eight Sent, eight Delivered, and one historical Failed. No row-level Delivered event was tied to this synthetic action, so this verifies successful workflow execution after the dedicated-domain setup, not receipt or raw-header authentication of that specific message.
- **Verified post-configuration row-level delivery and delayed routing — 2026-08-05:** A second synthetic Opportunity containing no real customer data was created at 9:31 PM ET and left in `New Quote Request`. GoHighLevel Email Analytics records the immediate hello message Delivered at 9:31 PM and the conditional Alex message Delivered at 9:36 PM. Signed-in Alex Outlook received the 9:36 PM message with visible PaintSwitch sender, a visible sending/routing address under `mg.paintswitch.com`, approved subject, and approved internal-alert body. This verifies the post-configuration row-level provider delivery and delayed routing plus Alex inbox receipt. It does not verify raw-header authentication results, warmup completion, notification-failure recovery, human response, or Production operation.
- **Verified legal/privacy preflight with unresolved gates — 2026-08-05:** Current source substantially matches the Confirmed Privacy, Terms, age, project-contact, automated-SMS-off, retention, data-use, vendor, and no-targeted-advertising requirements. The Virginia fictitious-name filing and required legal-identity/disclosure treatment remain unverified; the documented privacy-request and 12-month unconverted-lead procedures have not been rehearsed; provider access roles are not fully verified; and retention for notification email, provider logs, analytics, active/completed projects, and other record classes remains TBD. This preflight is not professional legal advice or approval.
- **Verified final-candidate local QA with unresolved release gates — 2026-08-05:** Source inspection and local built-route smoke found no new P0 code defect. `/`, `/privacy`, and `/terms` returned `200`; the styled not-found route, security headers, no-Jen/private-address/MHIC/unsupported-pricing boundary, and local asset use passed. Firefox/WebKit, screen-reader, monitoring/failure-alert, rollback rehearsal, Production promotion, and Production end-to-end evidence remain incomplete. The live apex still serves the prior build, so this local result is not launch acceptance.
- **Owner-reported / Needs verification — 2026-08-02:** After private password entry, GoDaddy displayed “Sorry, we can't create this user.” The owner chose to pause email setup and return to it later. The exact cause, entitlement/domain association, Microsoft 365 organization state, and recovery path remain unverified.

## Current development assessment

- The repository is at an in-progress lead-generation beta implementation stage. Protected-Preview success, retained-submission recovery, and hosted rate-limit threshold paths are verified, but Production lead delivery, quoting, and booking are not operational.
- Decision D-018 confirms a lead-generation beta before instant quote and checkout. Its four-service frontend, branded intake, manual-review messaging, placeholder-review removal, validation boundary, secure GHL client/mappings, durable delivery coordinator, and one synthetic Preview provider-path execution are verified. PR #2 is merged and the resulting Production deployment serves the updated homepage and safeguarded widget. Production lead delivery remains in the previously verified fail-closed state, and the earlier immutable enabled Preview remains access-protected.
- D-059 supersedes D-023's expired August 8 target with the weekend of 2026-08-15 through 2026-08-16 for the full editorial design and safeguarded `Switch bot`. D-023's one-hour-per-day owner limit remains. The target is not launch-readiness evidence or permission to bypass any applicable gate.
- Decision D-023 also confirms a custom branded PaintSwitch quote-request form, a secure server-side GoHighLevel connection, and removal of placeholder reviews. The form, review removal, private integration, Sensitive Vercel GHL and Upstash configuration, all seven field destinations, server-side connection code, controlled Preview website-to-provider paths, retained-submission recovery, hosted rate-limit threshold, and owner-confirmed notification receipt are verified. Broader failure/concurrency coverage, enabled Production lead-delivery release, and Production lead acceptance remain absent.
- The prior **49%** readiness snapshot is retired. Post-configuration row-level hello/Alex delivery, routed Alex receipt, both stages/branches, Preview safeguards, dependency checks, dedicated sending-domain/live DNS configuration, Production application deployment, and widget-opening evidence are milestones, not Production launch acceptance. Current blockers remain domain-warmup completion and raw-header authentication proof, the earlier failure's root cause and recovery path, broader hosted failure/concurrency coverage, privacy/retention and professional/legal review, Virginia fictitious-name filing/required-disclosure verification, operational activation/transition acknowledgment, in-coverage human-response rehearsal, enabled Production lead delivery and end-to-end QA, Production chatbot-response acceptance, any promoted-traffic release, and final browser/monitoring launch QA.
- The approved quote, routing, checkout, and booking model is documented but not implemented.
- D-055 through D-057 confirm the sophisticated/editorial tone, supplied palette direction, and transformation-through-color storytelling. D-058 completes owner visual review of the exact current local beta candidate, including its Georgia/system typography, token assignments, text-only interim wordmark, and generated/local hero explicitly labeled as an illustrative color study rather than customer work. Local regression, responsive-browser, key-interaction, and contrast checks pass. Final paint-roller logo artwork, an exact warm-gold value, authentic permissioned PaintSwitch before/after project assets/claims, final supporting copy beyond the displayed copy, hosted Preview, Production verification, merge/deployment, and promoted-traffic approval remain incomplete or separate.
- The flat $125 fuel/materials adjustment is **Confirmed**, but critical base pricing, eligibility, checkout, and policy decisions remain **TBD**, so a safe firm-pricing implementation is still blocked on product decisions.
- A customer-facing chatbot is **Confirmed**. GoHighLevel Conversation AI, Prompt Based/Start from Scratch, OpenAI GPT-4.1, D-052 prompt safeguards, D-053 controls, and D-054's `Switch bot` identity are approved; the paid AI plan is enabled. D-059 makes the safeguarded bot part of the intended next-weekend full-design launch after acceptance rather than an optional later release. Commit `85117a1` contains the exact WSS allowance and passed local, PR, and one bounded hosted real-time display check; earlier `649f06d` evidence and the manual synthetic deletion rehearsal remain valid. Squash-merged commit `7fb7359ccd5d1518d2dd4763e34350da9168d0cb` is live in Production, where homepage-only presence and opening passed; squash-merged commit `0dd1c74f2a0d2b5260f36061b573986eb5b57126` aligns Privacy with the provider-managed `Switch bot` title and AI introduction. No PaintSwitch chatbot API, model client, transcript persistence, summary/export workflow, automatic handoff, action, booking, payment, or additional channel exists. No Production chat message was sent, so Production reply/latency, exact two-second delay/cap/sleep runtime, broad safety, mobile/accessibility, both kill-switch shutdown checks, and provider-enforced retention/anonymization remain pending.
- GoHighLevel and CRM integration are **Confirmed**. The account/sub-account, pipeline/stage, fields, integration/permissions, Vercel/Upstash configuration, corrected Business Profile, dedicated sending-domain/live DNS configuration, corrected received-sender evidence, published workflow state, controlled manual and Preview execution, post-configuration row-level hello/Alex delivery, and routed Alex receipt are verified. My Staff still has no verified location user. Enabled Production delivery, domain-warmup completion, raw-header authentication evidence, notification-failure recovery, human-response operation, payment configuration, and release phasing remain unimplemented or **TBD**.
- The PaintSwitch owner is the confirmed primary beta lead owner. Preview intake, CRM records, immediate hello delivery, both stages/branches, no-customer-action behavior, post-configuration row-level hello/Alex delivery, and routed Alex receipt pass. Privacy/Terms and the runbook are implemented. Transition acknowledgment, operational backup activation, in-coverage human-response rehearsal, privacy/retention rehearsal, professional/legal review, Virginia fictitious-name filing verification, Production operation, and complete launch evidence remain absent.
- Current source has a token-checked 60-second lock, release of an acquired lock after initial-state failure, durable phases with 30-day expiry, at-most-one automatic Opportunity creation, ambiguous-result reconciliation, explicit rejected-create recovery, atomic fixed-window rate limiting, streamed response caps, and sanitized submission-ID reporting. Sixty-one tests pass locally on Next.js `16.3.0`, including the prior delivery and public-content coverage plus the chatbot and exact WSS-origin contracts. Commit `85117a1` passed fresh GitHub `Verify`, Vercel deployment, and Preview Comments checks, and its hosted Preview exercised one bounded no-reload chatbot response. PR #2 was squash-merged as `7fb7359ccd5d1518d2dd4763e34350da9168d0cb`; the Production deployment reached `Ready`, the apex returned `200`, and live CSP/security-header inspection plus homepage-only widget opening passed. Earlier controlled hosted evidence verifies the lead success path, retained-submission recovery without duplicate provider records, technical-state completion, owner-notification `Success`, and the five-request threshold with a blocked additional request returning `429`. None of this is a strict cross-provider exactly-once guarantee or Production lead/chat-response acceptance. Broader lead failure modes/concurrency and enabled Production lead delivery remain blockers.
- External A2P registration or approval remains a schedule risk. No phone number, A2P registration, or verified legal sender-identity packet exists. D-027 permits beta launch with SMS disabled while A2P is pending; exact legal wording/approval and other applicable beta gates remain required. Provider/carrier readiness does not replace final legal review.
- Vercel is the confirmed beta host. On 2026-08-04, a signed-in dashboard check verified the `Paint Switch` team labeled `Pro` and the `paintswitch-web` project within that team. Domain health, the `www` redirect, branch-to-Preview automation, and Ready hosted Preview `https://paintswitch-44fjlzkoy-paint-switch.vercel.app` for commit `85117a1` are verified; PR #2's fresh Vercel checks passed. Earlier Preview `https://paintswitch-9vzqs7zmi-paint-switch.vercel.app` for commit `649f06d` remains historical chatbot debugging evidence, and earlier application commit `beb6781` was restored to disabled lead delivery after its protected operational tests. PR #2 was squash-merged as `7fb7359ccd5d1518d2dd4763e34350da9168d0cb`; Production deployment `paintswitch-ghh6fv5dl-paint-switch.vercel.app` reached `Ready`, and the live apex serves the merged homepage with the widget present only on `/`. Billing administration, rollback, monitoring, enabled Production lead delivery, combined editorial-design release, and Production chatbot reply/latency acceptance remain unverified. D-059 resolves launch intent, not those runtime facts.
- Collectible payments, working proposals/bookings, measurable marketing leads, and first-project operational readiness are Confirmed end-state outcomes but are not implemented or verified.
- Proposed payment details, analytics behavior, detailed chatbot behavior, customer-scheduling mechanics, human handoff, and photo-upload capabilities require approval before implementation.

## Next status updates

Update this document when:

- a product journey becomes connected end to end;
- a placeholder becomes production-ready;
- lint, build, tests, dependencies, or deployment are reverified;
- a material security or dependency risk is triaged;
- a release changes what is implemented, missing, or operational;
- a canonical business decision exposes or resolves a code/documentation conflict.
